# Server Configuration
NODE_ENV=development
PORT=3000

# MongoDB Configuration
# Option 1: Use MongoDB URI (recommended for MongoDB Atlas or full connection string)
MONGODB_URI=mongodb://localhost:27017/acadmark_attendance

# Option 2: Use individual connection parameters (used if MONGODB_URI is not set)
DB_HOST=localhost
DB_PORT=27017
DB_NAME=acadmark_attendance

# For MongoDB Atlas, use a connection string like:
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/acadmark_attendance?retryWrites=true&w=majority

# Session Configuration (CHANGE THIS IN PRODUCTION!)
SESSION_SECRET=7718

# Admin Credentials (CHANGE THESE IN PRODUCTION!)
ADMIN_USER=admin@acadmark
ADMIN_PASSWORD=admin123

# Campus Location (optional - for geolocation features)
CAMPUS_LATITUDE=19.0760
CAMPUS_LONGITUDE=72.8777
CAMPUS_RADIUS_METERS=500
